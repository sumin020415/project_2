import MapLoader from '../components/MapLoader'

const Home = () => {
    return(
        <section id="home" data-page="home">
            <MapLoader />
        </section>
    )
}

export default Home